---
source: telegram_photo
date: 2026-08-27T14:04:38+00:00
user_id: 822266080
username: AlexeyDTC
image_file: 20260827_140438_AlexeyDTC_msg4886.jpg
original_image: 20260827_140438_AlexeyDTC_msg4886.jpg
final_location: ../assets/images/why-did-i-create-my-own-terminal-multiplexer/systemd-orderly-shutdown-diagnosis.jpg
---

1. **Type**: Terminal screenshot (or chat interface displaying terminal-style output)

2. **Main content**:
   A diagnostic log or developer note explaining the behavior of `tmux` servers during a system shutdown and restart. It clarifies that the servers were not killed by an Out-Of-Memory (OOM) event, but rather stopped deliberately by the user’s systemd manager as part of an orderly shutdown process. The text also mentions enabling the `Linger` option, which affects service persistence across user logouts, and notes that the author is investigating why the `t list` command may be misleading.

3. **Visible text** (OCR captured from image):

```
The tmux servers were not OOM-killed. At 15:57 the entire user systemd
manager performed an orderly shutdown: it explicitly stopped every
tmuxctl-server-* service and every pane scope, then a fresh user
manager started at 15:58. Linger is enabled, so this points to an
explicit user-manager restart/exit, not logout. I’m tracing what
requested that restart and then I’ll address the misleading t list
behavior.
```

4. **Potential use**:
   This excerpt would support articles or notes on:
   - Debugging Unix/Linux service lifecycle (especially `systemd` + `tmux`)
   - Understanding `Linger` behavior in systemd user services
   - Troubleshooting terminal multiplexer persistence issues
   - DevOps/Sysadmin incident post-mortems or debugging logs
   - Documentation for tools like `tmux`, `systemctl`, or custom daemon managers

This snippet is highly technical and suitable for developer documentation, sysadmin notes, or debugging journals.