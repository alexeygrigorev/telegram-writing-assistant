---
source: telegram
date: 2026-03-11T19:30:04+00:00
user_id: 822266080
username: AlexeyDTC
---

sequenceDiagram
    participant C as Codespace (AWS SDK)
    participant L as Lambda Function URL
    participant S as AWS STS

    C->>L: GET / (Authorization: token)
    L->>S: AssumeRole(workshop-participant-role)
    S-->>L: Temporary credentials (15 min)
    L-->>C: AccessKeyId, SecretAccessKey, Token, Expiration

    Note over C: SDK caches credentials

    C->>L: Auto-refresh before expiry
    L->>S: AssumeRole
    S-->>L: New temporary credentials
    L-->>C: Fresh credentials